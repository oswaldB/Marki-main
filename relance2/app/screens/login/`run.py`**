"""Point d'entrée de l'application."""

from app import create_app, init_db

if __name__ == '__main__':
    # Initialisation de la base de données
    init_db()
    
    # Création et lancement de l'app
    app = create_app()
    app.run(debug=True, host='0.0.0.0', port=5000)